import styled from "styled-components";

const AboutUsRoot = styled.div`
  position: relative;
  text-transform: capitalize;
  font-weight: 600;
`;
const AboutUs = () => {
  return <AboutUsRoot>About Us</AboutUsRoot>;
};

export default AboutUs;
